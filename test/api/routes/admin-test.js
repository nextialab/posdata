var chai = require('chai');
var expect = chai.expect;

describe('Test test', function () {
    it('Should test that 0 === 0', function () {
        var zero = 0;
        expect(zero).to.equal(0);
    });
});